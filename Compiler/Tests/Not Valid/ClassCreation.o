class A is
end

class B extends A is
    this(Integer i) is
        var b is i
    end
end

class C is
    this is
        // Missed parameter
        var b1 is B()
        
        // Excess parameter
        var b2 is B(1, 2)
    end
end