class A is
end

class B extends A is
    this(Integer i) is
        var b = i
    end
end

class C is
    this is
        // Missed parameter
        var b1 = B()
        
        // Excess parameter
        var b2 = B(1, 2)
    end
end