class Program is          //invalid case
  var y: 9
end

class Access is
  var x: Program()
  this is 
    x.y := 5         // here we try to change x, but only reading is available
  end
  
end

// Доступ к переменным из-вне доступен только для чтения.