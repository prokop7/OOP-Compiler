class A[T] is
    this(t: T)
        var b: t
    end
end

class B[TKey, TValue] is
    this(k: TKey, v: TValue)
        var key: k
        var value: v
    end
end

class C is
    this is
        var a: A[Integer](4)
        var b: B[Integer, Boolean](1, true)
    end
end