class A is
    method Foo is
        return 5
    end

    this is
        var a: 0
    end
end

class B extends A is
    this(Integer i) is
        var b = i
    end
end

class C is
    this is
        var b = B(5)
        b.Foo()
    end
end