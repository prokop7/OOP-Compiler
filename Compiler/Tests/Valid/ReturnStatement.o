class Program is
    this is
        var a: 20
        a := Foo(12)
        Foo(0) // withour left part
        nullMethod(2) // nothing happens
    end
 	

 	// return normal expression
    method Foo(n: Integer): Integer is
        return n
    end

    // no expression in return statement
    method nullMethod(n: Integer) : is
        return 
    end
end