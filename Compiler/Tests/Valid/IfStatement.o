
// a) without else

class Program is
	var a: 0.5
	var b: false
	if a.Plus(0.5).toInteger.Equal(1)  
	then 
		b: true   
	end
end

// b) with else

class Program is
	var a: 0.5
	var b = 4.5
	var c: a.Plus(b)

	if  c.Equal(2)
	then 
		a := c.Div(2)
	else 
		a := 0
	end
end

// с) with empty Body

class Program is
	var a: 0.5
	var b: false
	if a.Plus(0.5).toInteger.Equal(1)  
	then 
		 
	end
end